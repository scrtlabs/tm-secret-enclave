enclave {
    from "sgx_tstd.edl" import *;
    from "sgx_stdio.edl" import *;
    from "sgx_fs.edl" import *;
    from "sgx_net.edl" import *;
    from "sgx_time.edl" import *;
    include "sgx_quote.h"
    from "sgx_backtrace.edl" import *;
    from "sgx_tstdc.edl" import *;
    from "sgx_tprotected_fs.edl" import *;

    from "sgx_env.edl" import u_getenv_ocall, u_setenv_ocall;

    trusted {

        public sgx_status_t ecall_health_check();
        public int64_t ecall_generate_random();
        public sgx_status_t ecall_submit_validator_set(
            [in, count=val_set_len] const uint8_t* val_set,
            uint32_t val_set_len
        );

        public sgx_status_t ecall_submit_block_header(
            [in, count=block_header_len] const uint8_t* block_header,
            uint32_t block_header_len
        );

        public sgx_status_t ecall_validate_encrypted_random(
            [in, count=encrypted_random_len] const uint8_t* encrypted_random,
            uint32_t encrypted_random_len
        );

    };

    untrusted {

    };
};
